# 验证合同

Status: specified, not executed. 已执行的5项数据清理测试不计入以下求解验证。

## 几何与证书

Q3专项：检查中心及六个1200米等角外围点；以凸二次端点界证明外环距离不超过968.902米。测试rho=1000/1800和扇区中线/分界线，数值检查只验证实现，不替代理论证明。确认原点必扫且不重复作为额外全局点；7200米只作无扰动纯覆盖基线，不是动态必走路线。对照SAFE<32360秒（含返回），更松覆盖包络33824秒。全局measure<=140。验证滚动调度：P空则剩余精确最短路；P非空则N20/N_useful/G/L_remain字典序；ever_detected=16取消剩余发现；clear后不默认返回发现点。对照A固定六边形、B最近邻、C无定位价值最短路。构造direction/near后追加七点no_signal的矛盾记录，状态不得由detected变certified_absent；缺点、拒绝或重复同点也不能伪造七点证书。未完成覆盖点在ever_detected<16时不得永久删除。

Q1采用解析测试：0/1测点无界、矛盾空集、点、线段、矩形、可实现等边三角形、跨0度、近乎平行与极远交点。至少100个固定种子随机一致几何，与独立半平面实现交叉核验；数值残差、LP自由变量和状态均核对。解析反例优先于随机通过数量。

Q2对首测可行源位置与未知固定半径生成共用状态集Z1，r两次检测保持同一值；方向误差含两端点、0和内部样本；必须包含near、direction(b)与no_signal。核验F_no不等于F1减去1000米圆；核验C_core ⊆ F_full_1000 ⊆ C_guaranteed；核验未裁剪扇形MEC半径为1500/(2 cos 1°)且rho(F1)不超过该值；核验750.114米不当作接收半径。候选为A/B/C三层，仅对优胜局部做24→48→96加密，记录J变化及推荐变化。不得再使用以s为心的20–3000米固定网格。有限采样不得称连续全局最优。面积覆盖率eta不得标为检测概率。代理模型未启用，代理准入不适用；工程分辨率由离散化误差和留出案例差异判断，可辨识优势未达到时只列非劣候选。

覆盖证书以报告中的解析定理为主，另对圆边界、半径1000、朝向外法线、网格角点/边点、360个方向和固定随机位置作反例搜索。网格采样未发现反例不能替代定理。光学网格对每格四角、全长1500边界及误差±1/±1.005度核验最大距离；完整路径和返回计时独立计算。

## 案例级公平比较

简单基线SAFE；ACTIVE、ORDER只作待晋升候选。每场景共同种子、共同合法数据、同一预算，以完整案例为独立统计单位，检测行为为重复测量。先20个开发案例（种子11000..11019），冻结参数后100个留出案例（21000..21099），Q3/Q4各一套。每种策略每个案例运行一次；伪随机场由案例种子、源频道及精确请求坐标决定，同点同频道必须复用误差，不能按调用序号重新采样。真实模拟器案例不能重放时不冒充配对设计。

另设确定性压力矩阵：N=10/16、全部最小半径、圆边缘、中心、聚簇、贴近测点5/20米边界；Q4全向与定向混合、定向比例极端、外向/切向/交替朝向。误差分布比较零误差、固定±1度、空间相关有界场及均匀场，不假定真实概率。

指标按字典序报告：清除全部的案例数、证书正确率、未完成原因；成功案例T及T/K；所有案例K/N、墙钟、调用数和失败清除次数。不能只删去失败案例后比较条件均值。成对T差异只在两策略共同成功案例中计算并注明分母，同时报告各自失败数。案例级bootstrap 2000次，种子31000，给差异区间；它是自建场景下经验不确定性，不是全场景保证。

## 接口与现实回验

先不联网的协议替身测试：顺序请求、固定点固定误差、clear不切频、拒绝时不重置T、near、成功清除、幂等重试、截止和exit失败保留主错误。原约束回代检查坐标、频道、时间、源真值始终包含于安全外包。

之后使用已提供的官方接口开展演练。先1个Q3和1个Q4全闭环，再各至少5次演练验证稳定性；这是计划数量，不是已跑数量。记录p50/p95/p99延迟、总墙钟和最坏负载估计，检查5238级调用是否能在剩余预算内完成。不得从本地模拟器替身速度推断官方接口速度。现有客户端5秒预留小于可能3次5秒超时加退避，实现时须由适配层预留更大预算并测试；本轮不改原客户端。

只有演练通过、策略冻结且用户明确授权后，才运行每问3次正式测试，保留原名jlog。算法只能读取官方动作反馈；测试结束的N只用于离线评分。预算未完成为right-censored，不填完成时间。

## 运行预算与停用

实现计划使用本机Python+NumPy/SciPy，初步离线批量预算30分钟，单案例CPU预算5秒起测并记录实际值；若不足先缩小开发批次，不将未跑留出集标为通过。Q2评价预算每决策0.1秒作为待测目标，超时选择保守候选或SAFE。墙钟前缀剩余量是强约束，虚拟600秒局部预算不能替代墙钟。

ACTIVE晋升要求：几何与接口硬约束全通过，覆盖/清除能力不退化，且同预算案例中有可解释的效率或其他职责增量。失败即保留SAFE，不预设固定百分比收益。任何清除证书假阳性、角包络漏真值或正式机会误触为阻断。
