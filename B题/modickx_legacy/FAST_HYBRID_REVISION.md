# FAST/HYBRID主动定位

并行增加FAST_ACTIVE与HYBRID_ACTIVE，不删除ROBUST的J(q)定义，不改七点坐标、Q1、Q4、225点SAFE。第一次direction后C_1=C_guaranteed必非空。统一C_k={q:∀(g,r)∈Z_k, ||q-g||<=r}。清除证书仍为near或rho(F)<=20。Q3接口strategy∈{SAFE,ROBUST,FAST,HYBRID}，正式策略待同案例演练。实现：src/active.py。
