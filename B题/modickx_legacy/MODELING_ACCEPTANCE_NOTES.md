# 建模阶段验收说明

状态：evidence_valid / pending_human_review；不是求解验证完成。

Q2已按用户要求重构为分层鲁棒选点，并改为以F1最小覆盖圆为中心的局部候选：理论扇形覆盖半径1500/(2 cos 1°)≈750.114米（几何尺度，不是接收半径）；C_core=B(c1,1000-rho1)且C_core ⊆ F_full_1000 ⊆ C_guaranteed；删除20–3000米固定网格。三分支J(q)与同一未知r未改。c1不是最终第二点。详见Q2_REVISION.md与MODELING_REPORT.md第3节。Q1、第4节225点SAFE、Q3七点、Q4定向覆盖未改。

Q3七点集合与连续覆盖证明未改。访问顺序改为滚动重规划：P空则剩余点精确最短Hamilton路径；P非空则Q2的J字典序选下一覆盖点。7200米降为无扰动纯覆盖基线，不是在线必走路线。对照SAFE界仍写32360秒（含返回发现点）；更松覆盖包络33824秒。ever_detected=16时由N<=16取消剩余发现任务。详见Q3_REVISION.md。连续覆盖最坏距离968.9015717043836米；不是最少点数证明。此前验收描述中的Q4数值保持不变，本次须重新校验并开启新版审核。

本轮已解析上一审题检查点，进入comp-modeling。已读取模型卡库catalog、鲁棒/Pareto族和模型卡schema，逐项实例化MODEL_CARD.md。模型卡结构验证ready；Modickx建模证据检查passed=true，无缺失产物与失败检查。本地字典未发现，独立bzd-modeling-ideas入口未发现，兼容候选登记由本会话编写，不伪造工具调用。

人工复核：题面图1/2已渲染回看；P4构造证明具有75.7米以上朝向余量及75.7米以上距离余量；光学网格覆盖半径10sqrt(2)<20米。等边三角形反例已给出可实现的2度楔构造。官方客户端没有move，已将返回点的clear失败动作及3秒额外成本加入证明。

算术复算（不是策略实验）：光学蛇形完整移动5992.49490191742米，加226次clear费用后单源上界1878.498980383484秒，小于1880秒。Q4加原点额外扫描按同路径精算上界50174.80619607483秒，小于50200秒；ACTIVE最多再加9600秒，小于60000秒。主文用更保守的整数上界，未声称实测达到此值。

尚待下阶段：几何与策略代码、独立数值验证、实际演练吞吐、预算失败处理、ACTIVE增量比较。G1-G10矩阵中的未来工作保持pending，不因结构检查通过而晋升科学结论。官方正式测试和最终论文尚未执行。本地文献PDF元数据及关键前提已核读，在线出版状态核验仍未完成。

本次请求人工确认机制、假设、公式、变量、约束、求解及验证路径。确认后才可进入comp-code；此确认不授权正式测试。
