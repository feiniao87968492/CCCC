# 稳定语义身份与独立统计单位

动作主键为(session_id, source_line)，幂等身份为(session_id, request_id)。源身份为(session_id, channel)；策略身份须绑定代码版本与参数元组，显示名称不是身份。独立统计单位为完整随机案例；动作、测点和频道结果是案例内重复测量。当前两个接口演练会话不用于独立策略效果估计。未来相同案例比较策略，以案例层成对统计，不能把12行当12个独立样本。
