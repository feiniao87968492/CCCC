# Q3动态调度修订

用户指定：七点集合P3与连续覆盖证明不变；删除默认固定顺序C→U0→…→U5作为在线必走路线；改为滚动重规划。不授权正式测试。

调度：原点仍先扫。P空则对剩余R枚举精确最短Hamilton路径（<=6!）。P非空则对每个剩余覆盖点算Q2的J_k，字典序最大化N20、N_useful、G，再最小化L_remain。发现direction后默认完成当前点扫描再规划；near可立即clear。ever_detected=16时由N<=16取消剩余发现任务。clear/SAFE后不默认返回发现点。7200米降为无扰动纯覆盖基线；32360秒保留为含返回的对照上界。

Q1、Q2的J/C_core/三分支、七点坐标、覆盖证明、Q4、225点SAFE数学保证未改。
