# 问题2完整参考方案：主候选区与次候选区相结合

问题2的核心是：

> 已经在第一个检测点发现了一个全向干扰源，现在应该把第二个检测点放在哪里，才能既保证还能检测到它，又能通过两次测向尽量准确地定位它？

整体策略为：

\[
\boxed{
\text{第一次检测}
\rightarrow
\text{建立干扰源可能区域}
\rightarrow
\text{确定主候选区}
\rightarrow
\text{选择第二检测点}
\rightarrow
\text{更新定位区域}
\rightarrow
\text{必要时建立次候选区}
}
\]

---

# 一、第一次检测后，干扰源可能在哪里？

设：

- 第一个检测点为 \(S_1=(x_1,y_1)\)；
- 测得的示向度为 \(\theta\)；
- 干扰源为全向干扰源；
- 测向误差范围为 \([-1^\circ,1^\circ]\)。

由于第一次返回了有效方向，说明：

1. 干扰源距离第一个检测点大于5米；
2. 干扰源距离第一个检测点不超过其有效接收半径；
3. 有效接收半径最大为1500米；
4. 干扰源方向位于 \(\theta\pm1^\circ\)范围内；
5. 干扰源位于半径1800米的目标区域内。

因此，干扰源的可能位置区域为：

\[
F_1=
\left\{
g:
\begin{array}{l}
\|g\|\le1800,\\
5<\|g-S_1\|\le1500,\\
\arg(g-S_1)\in[\theta-1^\circ,\theta+1^\circ]
\end{array}
\right\}
\]

通俗地说，\(F_1\)就是：

> 以第一次检测点为顶点、方向误差为两侧边界、最远距离为1500米的狭长扇形区域。

---

# 二、为什么不能沿第一次方向继续测？

如果第二个检测点仍然沿着第一次示向方向布置：

```text
S1 ---------------- Q ---------------- 可能目标
```

那么两次观测方向近似相同，两个方向误差带几乎平行，交会定位效果很差。

因此，第二检测点不应简单地沿第一次方向继续前进，而应尽量：

- 位于干扰源可能区域的侧方；
- 与第一次检测点形成明显的横向位移；
- 使两次观测方向夹角尽量接近 \(90^\circ\)。

但是，第二检测点也不能离可能目标区域太远，否则可能收不到信号。

所以第二检测点要在以下因素之间平衡：

\[
\text{检测可靠性}
+
\text{定位精度}
+
\text{移动时间}
\]

---

# 三、主候选区：保证第二次尽量能检测到

## 1. 先确定每个可能位置对应的最小接收半径

设某个可能干扰源位置为 \(g\)，记：

\[
\rho=\|g-S_1\|
\]

干扰源的真实有效接收半径 \(r\)满足：

\[
1000\le r\le1500
\]

由于第一次在 \(S_1\)已经检测到该干扰源，所以还知道：

\[
r\ge \rho
\]

因此，对于位置 \(g\)，接收半径的最小可能值为：

\[
r_{\min}(g)=\max(1000,\|g-S_1\|)
\]

分两种情况：

### 当 \(5<\rho\le1000\)时

\[
r_{\min}(g)=1000
\]

### 当 \(1000<\rho\le1500\)时

\[
r_{\min}(g)=\rho
\]

这里不是说干扰源实际接收半径一定等于 \(\rho\)，而是说：

> 在已知信息下，为了保守，至少要按照 \(\rho\)来考虑。

---

## 2. 对每个可能目标位置建立接收圆

假设第二检测点为 \(q\)。

如果希望第二次检测一定能收到信号，就需要：

\[
\|q-g\|\le r_{\min}(g)
\]

对于每一个可能位置 \(g\in F_1\)，以 \(g\)为圆心、\(r_{\min}(g)\)为半径画一个圆。

那么所有这些圆的交集就是主候选区：

\[
\boxed{
C_{\mathrm{main}}
=
\bigcap_{g\in F_1}
B\left(g,\max(1000,\|g-S_1\|)\right)
}
\]

其中：

\[
B(g,r)=\{q:\|q-g\|\le r\}
\]

表示以 \(g\)为圆心、半径为 \(r\)的圆。

---

## 3. 主候选区的实际含义

如果：

\[
q\in C_{\mathrm{main}}
\]

那么无论真实干扰源位于 \(F_1\)中的哪个位置，第二次检测都不会因为距离太远而无法收到信号。

但是需要注意：

\[
q\in C_{\mathrm{main}}
\]

只能保证：

> 第二次不会因距离原因完全失去信号。

它不能保证：

- 第二次一定得到 `direction`；
- 定位区域立即小于20米；
- 机器狗当前位置就是清除位置。

第二次也可能返回：

- `direction`：检测到方向；
- `near`：已经距离目标不超过5米。

---

# 四、中心线近似下的两个圆交集

如果暂时忽略 \(\pm1^\circ\)方向误差，把干扰源简化为第一次示向方向上的线段：

\[
0\le\rho\le1500
\]

那么主候选区可以近似理解为：

\[
C_{\mathrm{main}}
\approx
B(S_1,1000)
\cap
B(S_{1000},1000)
\]

其中 \(S_{1000}\)表示沿第一次示向方向、距离 \(S_1\)为1000米的位置。

在局部坐标系中，如果：

\[
S_1=(0,0)
\]

第一次测向方向为 \(x\)轴正方向，则：

\[
S_{1000}=(1000,0)
\]

所以：

\[
C_{\mathrm{main}}
\approx
B((0,0),1000)
\cap
B((1000,0),1000)
\]

这是两个半径1000米圆的交集，形状类似一个透镜。

但严格方案不能只使用这两个圆，因为真实干扰源位于一个有宽度的扇形中，而不是一条中心线。因此实际应使用前面的扇形圆交集定义。

---

# 五、如何计算主候选区

主候选区是连续区域，实际可以采用数值方法近似。

## 方法1：扇形网格采样

在 \(F_1\)中采样一批可能干扰源位置：

\[
g_{ij}
=
S_1+
\rho_i
\begin{pmatrix}
\cos\varphi_j\\
\sin\varphi_j
\end{pmatrix}
\]

其中：

\[
\rho_i\in\{5,25,50,\ldots,1500\}
\]

\[
\varphi_j\in[-1^\circ,1^\circ]
\]

然后对每个采样点计算：

\[
r_{ij}=\max(1000,\|g_{ij}-S_1\|)
\]

要求候选点 \(q\)满足：

\[
\|q-g_{ij}\|\le r_{ij}
\]

所有这些约束的交集，就是主候选区的数值近似。

---

## 方法2：候选网格筛选

在目标区域内布置许多可能的第二检测点 \(q\)，逐一判断：

\[
\max_{g\in F_1}
\left(
\|q-g\|-r_{\min}(g)
\right)
\le0
\]

满足条件的点组成主候选区。

这种方法实现简单，适合模拟器程序。

---

# 六、主候选区内部还要进一步筛选

主候选区解决的是“能不能继续检测”，但不同位置的定位效果不同。

因此，从主候选区中继续选择优选区域：

\[
C_{\mathrm{prefer}}\subseteq C_{\mathrm{main}}
\]

优选时主要考虑以下因素。

---

## 1. 与第一次测向方向不能近似共线

设第一次示向方向单位向量为：

\[
u=(\cos\theta,\sin\theta)
\]

第二检测点 \(q\)相对于第一次检测点的横向距离为：

\[
d_{\perp}
=
\left|
(q-S_1)\times u
\right|
\]

如果 \(d_{\perp}\)太小，说明第二个检测点仍然接近第一次方向线，定位效果较差。

因此应排除：

\[
d_{\perp}<d_{\min}
\]

例如可以设置：

\[
d_{\min}=300\sim500\text{米}
\]

---

## 2. 尽量让交会角接近 \(90^\circ\)

对于可能目标位置 \(g\)，第一次和第二次的观测方向分别为：

\[
g-S_1
\]

和：

\[
g-q
\]

设两者夹角为 \(\phi(q,g)\)。

希望：

\[
\phi(q,g)\approx90^\circ
\]

原因是：

- 夹角接近0°：两条方向线近似平行，定位误差很大；
- 夹角接近90°：两条方向线交会较稳定；
- 夹角接近180°：同样容易出现不稳定。

因此可以优先选择使最坏交会角较好的点，例如最大化：

\[
\min_{g\in F_1}
\min\left\{
\phi(q,g),\,
180^\circ-\phi(q,g)
\right\}
\]

---

## 3. 移动距离不能太长

如果机器狗当前位于 \(M\)，移动到候选点 \(q\)的时间为：

\[
T_{\mathrm{move}}=\frac{\|q-M\|}{5}
\]

在定位效果相近的情况下，应优先选择距离当前机器狗较近的点。

---

## 4. 尽量选择目标区域内部或附近的点

如果候选点位于目标区域外很远的位置，虽然不一定违反接口规定，但可能：

- 移动距离很大；
- 增加运行时间；
- 不利于后续搜索路线。

因此可以将目标区域外的候选点降低优先级。

---

# 七、推荐的第二检测点选择规则

可以采用分层选择，而不是简单地把各指标混成一个分数。

## 第一步：检测可靠性筛选

优先保留：

\[
q\in C_{\mathrm{main}}
\]

如果主候选区为空，则保留能够覆盖较多可能目标位置的点。

---

## 第二步：定位效果筛选

在剩余点中，删除：

- 与第一次方向近似共线的点；
- 交会角过小的点；
- 可能导致后验区域仍然很大的点。

---

## 第三步：移动代价筛选

如果有多个点定位效果接近，则选择：

\[
\|q-M\|
\]

较小的点。

因此选择原则可以写成：

\[
\boxed{
\text{先保证可检测，再追求定位准，最后减少移动时间}
}
\]

---

# 八、第二次检测后的结果处理

到达选定的第二检测点 \(q\)后，仍然要根据实际反馈进行处理。

---

## 情况1：返回 `near`

说明：

\[
\|q-g\|\le5\text{米}
\]

此时机器狗已经非常接近干扰源，应该直接调用：

```text
/clear
```

不需要继续寻找新的检测点。

---

## 情况2：返回 `direction`

设第二次测得示向度为 \(b\)。

第二次测量产生新的方向误差带：

\[
W_2=
\left\{
g:
\arg(g-q)\in[b-1^\circ,b+1^\circ]
\right\}
\]

更新后的干扰源可能区域为：

\[
F_2=F_1\cap W_2
\]

如果考虑检测结果为 `direction`，还应加入：

\[
5<\|g-q\|\le r
\]

以及原来关于接收半径的约束。

通俗地说：

> 第一次测量给出一个扇形，第二次测量又给出一个扇形，干扰源只能位于两个扇形的重叠区域中。

---

## 情况3：返回 `no_signal`

因为第一次已经发现过该频道，所以不能认为干扰源不存在。

对于全向干扰源，第二次无信号通常表示：

- 第二检测点离目标太远；
- 超出了该干扰源的实际接收半径；
- 当前候选点选择不理想。

此时应：

1. 保留第一次的可能区域；
2. 根据“无信号”进一步排除部分位置；
3. 换一个候选点；
4. 再次检测同一频道。

在严格模型中，无信号会排除满足：

\[
\|q-g\|\le r
\]

的状态，但不能简单地把所有距离小于1000米的点直接排除，因为真实半径可能大于1000米，而且半径 \(r\)是同一个固定但未知的值。

---

# 九、次候选区：第二次检测后进一步提高精度

主候选区主要用于保证第二次检测可靠。

如果第二次返回了 `direction`，就能得到更小的定位区域 \(F_2\)。

如果 \(F_2\)还比较大，则围绕 \(F_2\)重新设计第三个检测点。

这就是次候选区。

定义：

\[
C_{\mathrm{secondary}}
=
\bigcap_{g\in F_2}
B\left(g,r_{\min,2}(g)\right)
\]

其中 \(r_{\min,2}(g)\)是结合前两次检测信息后，位置 \(g\)对应的最小相容接收半径。

简单理解：

> 主候选区是根据第一次信息为第二次检测准备的；次候选区是根据前两次信息为第三次检测准备的。

---

## 次候选区的选择重点

次候选区不一定要保证对所有可能位置都能收到信号，因为经过两次检测后，可能区域已经小很多。

此时可以更加注重：

- 缩小定位区域；
- 让第三次观测方向与前两次明显不同；
- 使新的交会角更理想；
- 减少移动距离；
- 尽快达到20米清除条件。

---

# 十、什么时候停止继续定位？

目标不是无限提高定位精度，而是满足清除条件。

设当前干扰源可能区域为 \(F\)，定义其最小包围圆半径：

\[
\rho(F)=
\min_c\max_{g\in F}\|c-g\|
\]

如果：

\[
\boxed{\rho(F)\le20\text{米}}
\]

则存在一个清除点 \(c\)，使得：

\[
\|c-g\|\le20\text{米},
\qquad \forall g\in F
\]

此时机器狗可以：

1. 移动到清除点 \(c\)；
2. 调用 `/clear`；
3. 清除该频道的干扰源。

注意：

- 区域直径小于20米可以作为一种参考；
- 但严格的清除判断应使用最小包围圆半径；
- 不能因为“区域已经比较小”就直接在当前位置清除。

---

# 十一、精度和时间如何取舍？

问题2中的第二检测点不能只追求理论上最准确，还要考虑机器狗的实际运行时间。

每次新增检测的时间成本包括：

\[
T_{\mathrm{extra}}
=
\frac{\text{移动距离}}{5}
+
\text{换频道时间}
+
5\text{秒检测时间}
\]

因此，实际应遵守以下原则。

---

## 原则1：先保证不漏检

在问题3中，七个全局搜索点承担的是“确保发现全向干扰源”的任务，这部分优先级最高。

问题2的主动定位不能替代全局搜索覆盖。

---

## 原则2：达到清除精度后立即停止

如果已经满足：

\[
\rho(F)\le20
\]

就不再继续进行无线电测向。

---

## 原则3：已经返回 `near`时直接清除

`near`说明距离不超过5米，继续测向没有意义。

---

## 原则4：如果增加一次测量不能明显缩小范围，就不要盲目继续

例如：

- 当前定位半径为30米；
- 再测一次预计只能降到25米；
- 但需要移动1000米；

此时继续测量可能不如直接前往估计位置附近，再尝试清除。

---

## 原则5：问题4中允许更多方向探索，但要设置上限

定向干扰源可能因为方向不合适而返回无信号，因此需要多方向尝试。

但不能在同一个频道无限循环。应设置：

- 最大额外检测次数；
- 最大局部搜索时间；
- 超限后进入保底方案。

---

# 十二、问题2最终可以采用的完整算法

## 输入

- 第一次检测点 \(S_1\)；
- 第一次示向度 \(\theta\)；
- 目标圆域；
- 有效接收半径范围 \([1000,1500]\)；
- 测向误差范围 \([-1^\circ,1^\circ]\)；
- 机器狗当前点 \(M\)。

---

## 步骤1：构造第一次扇形可能区域

\[
F_1=
\left\{
g:
\|g\|\le1800,\ 
5<\|g-S_1\|\le1500,\ 
|\arg(g-S_1)-\theta|\le1^\circ
\right\}
\]

---

## 步骤2：构造主候选区

对每个 \(g\in F_1\)，计算：

\[
r_{\min}(g)=\max(1000,\|g-S_1\|)
\]

构造：

\[
C_{\mathrm{main}}
=
\bigcap_{g\in F_1}
B(g,r_{\min}(g))
\]

---

## 步骤3：生成主候选区中的优选点

删除：

- 靠近第一次检测点的点；
- 与第一次测向方向近似共线的点；
- 移动距离过长的点。

保留：

- 位于可能目标区域侧方的点；
- 交会角较好的点；
- 移动代价较小的点。

---

## 步骤4：选择第二检测点

按照以下优先级选择：

1. 在主候选区内；
2. 交会角接近 \(90^\circ\)；
3. 距离机器狗较近；
4. 便于后续路线继续执行。

---

## 步骤5：进行第二次检测

对同一个频道测量。

---

## 步骤6：根据反馈更新

- `near`：直接清除；
- `direction`：构造 \(F_2\)，判断是否满足清除条件；
- `no_signal`：更新可能区域，换候选点再次检测。

---

## 步骤7：必要时建立次候选区

如果：

\[
\rho(F_2)>20
\]

则围绕 \(F_2\)构造次候选区：

\[
C_{\mathrm{secondary}}
=
\bigcap_{g\in F_2}
B(g,r_{\min,2}(g))
\]

并继续进行第三次测量。

---

## 步骤8：达到清除条件后结束定位

当：

\[
\rho(F_k)\le20
\]

或者检测结果为 `near`时，停止无线电测向，移动到合适位置并调用 `/clear`。

---

# 十三、第二问的最终结论

问题2可以形成以下完整结论：

> 第一次测量后，干扰源应表示为一个考虑 \(\pm1^\circ\)示向误差的狭长扇形可能区域，而不是一条确定方向线。根据第一次已经成功接收信号这一事实，对于扇形内每个可能干扰源位置，可以确定其最小相容接收半径为 \(\max(1000,\|g-S_1\|)\)。将以各可能位置为圆心、以该最小接收半径为半径的圆求交，得到第二检测点的主安全候选区。主候选区主要保证第二次检测不会因距离过远而完全失去信号。随后在主候选区内优先选择位于可能目标区域侧方、与第一次示向方向不近似共线、交会角接近 \(90^\circ\)且移动距离较短的点。第二次检测后，根据新的示向度更新干扰源可能区域；若定位区域仍不足以保证20米清除条件，则围绕更新后的区域构造次候选区，继续主动选点。若返回 `near`或当前可能区域的最小包围圆半径不超过20米，则停止测向并进行清除。

最简洁地概括为：

\[
\boxed{
\begin{aligned}
&\text{主候选区：保证第二次能测到；}\\
&\text{优选区域：保证第二次测得较准；}\\
&\text{次候选区：根据第二次结果进一步缩小范围；}\\
&\text{清除判据：可能区域的最小包围圆半径}\le20\text{米。}
\end{aligned}
}
\]